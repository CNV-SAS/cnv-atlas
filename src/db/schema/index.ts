// Barrel del esquema Drizzle. Cada grupo de tablas de DATABASE.md va en su propio
// archivo (enums.ts, organizations.ts, patients.ts, ...) y se reexporta aqui, para
// que drizzle.config (schema) y el cliente db tengan un unico punto de entrada.
//
// Vacio a proposito en la sub-tarea 1 (setup): las 17 secciones del esquema se
// crean en la sub-tarea 2. No correr `drizzle-kit generate` hasta entonces.

export {};
