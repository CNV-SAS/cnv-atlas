// Tipos para frozen/atlas-protocolo.authorized.js (el GENERADO, el que corre). Su forma es la misma
// que la del original (motorProtocolo): las modificaciones autorizadas retiran/cambian contenido, no
// la firma. Se reexportan los tipos del original en vez de duplicarlos. Solo para tsc.
export { motorProtocolo } from "./atlas-protocolo";
